# probes_negative/ fixture — deliberately broken, NOT donor-faithful.
# Exercises v1.10 PR-D1's D0091 firing site in the Expr::Attribute
# arm (cross-dialect bare-attribute access, no call paren). The
# receiver is adjudicated as PandasFrame[X]; the bare `.write`
# attribute is in SPARK_DISCRIMINATOR_PROPERTIES (added in v1.10
# PR-D1) — D0091 fires on the attribute access itself.
#
# Failure mode exercised: v1.10 PR-D1 D0091 (crossDialectMethodMismatch)
# warning on bare-attribute (no call paren) path for the v1.10 Spark
# property surface. `write` is a Spark-only DataFrameWriter accessor;
# the receiver dialect is Pandas; the Expr::Attribute dispatch arm
# fires D0091. Pandas uses top-level `df.to_parquet(...)` instead.

from pykrete import PandasFrame


class FeaturesFrameNeg(Schema):
    sample: long
    score: double


def grab_writer(df: PandasFrame[FeaturesFrameNeg]) -> None:
# PROBE-EXPECTS: D0091 on "write" id=scikit-learn-pandas-neg-bare-attr-spark-write -- v1.10 PR-D1 D0091 bare-attribute path: `write` is in SPARK_DISCRIMINATOR_PROPERTIES but the receiver `df: PandasFrame[X]` is Pandas; Expr::Attribute arm dispatches without call paren and fires D0091; vacuity-falsifying because swapping `write` for `shape` (pandas-native) clears the fire
    handle = df.write
    _ = handle
