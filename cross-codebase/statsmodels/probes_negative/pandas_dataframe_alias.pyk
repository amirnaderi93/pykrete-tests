# probes_negative/ fixture — deliberately broken, NOT donor-faithful.
# Derived from the statsmodels-USER RegressionInput shape (sibling
# annotated/canonical/regression_pattern.pyk); the annotated sibling
# uses the canonical `PandasFrame[X]` v1.3 form, this fixture uses
# the deprecated `DataFrame[X]` alias to assert pykrete fires D0090
# per docs/design/pandas-support.md §6.
#
# Failure mode exercised: D0090 fires once per `DataFrame[X]`
# parameter annotation slot (v1.3 deprecation warning).

from pykrete import DataFrame


class RegressionInputDep(Schema):
    raw_y: double
    raw_x1: double
    subject_id: long


# PROBE-EXPECTS: D0090 on "DataFrame[RegressionInputDep]" id=statsmodels-pandas-neg-dataframe-alias -- DataFrame[X] alias on a parameter slot must fire D0090 per §6
def fit_with_alias(df: DataFrame[RegressionInputDep]) -> None:
    _ = df
