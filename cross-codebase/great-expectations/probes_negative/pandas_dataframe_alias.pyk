# probes_negative/ fixture — deliberately broken, NOT donor-faithful.
# Derived from the GE-USER ValidatedDataset shape (sibling
# annotated/canonical/expectation_pattern.pyk); the annotated
# sibling uses the canonical `PandasFrame[X]` v1.3 form, this
# fixture uses the deprecated `DataFrame[X]` alias to assert
# pykrete fires D0090 per docs/design/pandas-support.md §6.
#
# Failure mode exercised: D0090 fires once per `DataFrame[X]`
# parameter annotation slot (v1.3 deprecation warning).

from pykrete import DataFrame


class ValidatedDatasetDep(Schema):
    row_count: long
    value: double
    passes_check: bool


# PROBE-EXPECTS: D0090 on "DataFrame[ValidatedDatasetDep]" id=ge-pandas-neg-dataframe-alias -- DataFrame[X] alias on a parameter slot must fire D0090 per §6
def validate_with_alias(df: DataFrame[ValidatedDatasetDep]) -> None:
    _ = df
